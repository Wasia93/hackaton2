# Phase IV: Helm ConfigMap Template
# [Task]: T-020
# [From]: specs/phase4-kubernetes/spec.md FR-5

apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
data:
  BACKEND_URL: {{ .Values.config.backendUrl | quote }}
  OPENAI_AGENT_MODEL: {{ .Values.config.openaiModel | quote }}
  PROJECT_NAME: {{ .Values.config.projectName | quote }}
